/*void tree(int height){
	for(int i = 0; i < height; ++i)
	{
		int dist = height - i;
		for(int j = 0; j < dist; ++j)
		{
			printf(' ');
		}

		int num_star = i * 2 + 1;

		for(int j = 0; j < num_star; ++j)
		{
			printf('*');
		}
		printf('\n');
	}
}*/

void tree(int height){
	(a*lmao)();
	a + lmao();
	a = c + b;

	for(int i = 0; i < height; ++i)
	{
		int dist = height - i;
		for(int j = 0; j < dist; ++j)
		{
			printf(" ");
		}

		int num_star = i * 2 + 1;

		for(int j = 0; j < num_star; ++j)
		{
			printf("*");
		}
		printf("\n");
	}

	if(pong_tin) { lmao(); }
}
void harry_kane(){
	int a = 50;
	printf("%d ", a);
}

void main(){
	string lmao = "\";
	int a = c * b + c * b;
	int a = c * b + c * -d;
	//int a = c + d;
	return a;
}
